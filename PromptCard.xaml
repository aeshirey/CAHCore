<Window x:Class="CAHCore.PromptCard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CAHCore"
        mc:Ignorable="d"
        Title="PromptCard" Height="320" Width="200" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="canvas"/>
        <Button Grid.Row="1" Grid.Column="0" Name="btnCopyText" Content="Copy Card Text" Click="btnCopyText_Click" />
        <Button Grid.Row="1" Grid.Column="1" Name="btnNewCard" Content="Pick New Card" Click="btnNewCard_Click" />
    </Grid>
</Window>
